[config]
default_to_workspace = false

[env]
NO_STD_FLAGS = "--profile ${RUSTC_PROFILE} --timings"
COV_FLAGS = { value = "--workspace --profile test --ignore-filename-regex .*test.*", condition = { env_not_set = ["COV_FLAGS"] } }
RUSTDOCFLAGS = "-D warnings -D missing_docs"
# Base features included in all platform firmware builds.
# Keep this list in sync with the default features in Cargo.toml.
BASE_FEATURES = "compatibility_mode_allowed"
# Common rustflags used in all targets

[env.development]
RUSTC_PROFILE = "dev"

[env.release]
RUSTC_PROFILE = "release"

[tasks.install-tools]
description = """Installs the project tools pinned in the `[tools]` section of
`rust-toolchain.toml` at their specified versions. Each tool is installed with
`cargo binstall`, falling back to `cargo install` when a prebuilt binary is not
available. CI also installs and tests the tools in this section.

`cargo-binstall` (https://github.com/cargo-bins/cargo-binstall) is installed
automatically with `cargo install` when it is not already present.

Example:
    `cargo make install-tools`
"""
clear = true
install_crate = false
script_runner = "@duckscript"
script = '''
# cargo-binstall is used to install tools.
binstall_path = which cargo-binstall
if is_empty ${binstall_path}
    echo "==> cargo-binstall not found. Installing it with cargo install..."
    exec --fail-on-error cargo install cargo-binstall
end

toolchain_file = readfile rust-toolchain.toml
lines = split ${toolchain_file} "\n"

# Track whether the current line is inside the `[tools]` section. Parsing runs
# from the `[tools]` header until the next section header.
in_tools = set false
for line in ${lines}
    trimmed = trim ${line}
    is_section = starts_with ${trimmed} "["
    if ${is_section}
        if eq ${trimmed} "[tools]"
            in_tools = set true
        else
            in_tools = set false
        end
    elseif ${in_tools}
        is_comment = starts_with ${trimmed} "#"
        is_blank = is_empty ${trimmed}
        has_assignment = contains ${trimmed} "="
        # Only process `name = "version"` lines.
        if not ${is_blank}
            if not ${is_comment}
                if ${has_assignment}
                    kv = split ${trimmed} "="
                    name = array_get ${kv} 0
                    name = trim ${name}

                    version = array_get ${kv} 1
                    version_parts = split ${version} "\""
                    version = array_get ${version_parts} 1

                    result = exec cargo binstall -y ${name} --version ${version}
                    output = set "${result.stdout}${result.stderr}"
                    if not eq ${result.code} "0"
                        echo "  building ${name} ${version} from source (no prebuilt binary)..."
                        result = exec cargo install ${name} --version ${version}
                        output = set "${result.stdout}${result.stderr}"
                    end

                    if not eq ${result.code} "0"
                        echo "  FAILED       ${name} ${version}"
                        echo ${output}
                        exit 1
                    end

                    already = contains ${output} "already installed"
                    if ${already}
                        echo "  up-to-date   ${name} ${version}"
                    else
                        echo "  installed    ${name} ${version}"
                    end
                end
            end
        end
    end
end
'''

[tasks.individual-package-targets]
script_runner = "@duckscript"
script = '''
args = get_env CARGO_MAKE_TASK_ARGS

if is_empty ${args}
  exit
end

1 = array ""
2 = split ${args} ,
3 = array_concat ${1} ${2}
joined_args = array_join ${3} " -p "
release ${1}
release ${2}
release ${3}

joined_args = trim ${joined_args}
set_env INDIVIDUAL_PACKAGE_TARGETS ${joined_args}
release ${joined_args}
'''

[tasks.check_code_x64]
description = "Checks rust code for x64 build errors with results."
private = true
command = "cargo"
args = ["check", "--target", "x86_64-unknown-uefi", "--features", "ci_features,x64", "@@split(CARGO_MAKE_TASK_ARGS, )", "@@split(NO_STD_FLAGS, )",]

[tasks.check_code_aarch64]
description = "Checks rust code for aarch64 build errors with results."
private = true
command = "cargo"
args = ["check", "--target", "aarch64-unknown-uefi", "--features", "ci_features,aarch64", "@@split(CARGO_MAKE_TASK_ARGS, )", "@@split(NO_STD_FLAGS, )",]

[tasks.check_tests]
description = "Checks rust test code for build errors with results."
private = true
command = "cargo"
args = ["test", "--no-run", "--all-targets", "--all-features", "@@split(CARGO_MAKE_TASK_ARGS, )",]

[tasks.check]
description = "Checks rust code for errors. Example `cargo make check`"
clear = true
run_task = [{ name = ["check_code_x64", "check_code_aarch64", "check_tests"], parallel = true }]

[tasks.check-no-default-features-code]
description = "Checks rust code compiles without default features."
private = true
command = "cargo"
args = ["check", "--no-default-features", "@@split(CARGO_MAKE_TASK_ARGS, )"]

[tasks.check-no-default-features-tests]
description = "Checks rust test code compiles without default features."
private = true
command = "cargo"
args = ["test", "--no-run", "--no-default-features", "@@split(CARGO_MAKE_TASK_ARGS, )"]

[tasks.check-no-default-features]
description = "Checks rust code and tests compile without default features to catch feature-gate regressions."
clear = true
run_task = [{ name = ["check-no-default-features-code", "check-no-default-features-tests"], parallel = true }]

[tasks.test]
description = "Runs tests with native cargo test behavior. Example `cargo make test` or `cargo make test -p package_name -- --nocapture`"
clear = true
command = "cargo"
args = ["test", "@@split(CARGO_MAKE_TASK_ARGS,;)"]

[tasks.coverage-collect]
description = "Run tests and collect coverage data without generating reports."
install_crate = false
clear = true
# `coverage_attribute` is deliberately excluded from the default allow-features in
# .cargo/config.toml so ungated `#[cfg_attr(coverage, coverage(off))]` fails to compile. Coverage runs set the
# `coverage` cfg (set by cargo-llvm-cov), which needs the feature, so re-add it here.
# Note: RUSTFLAGS replaces build.rustflags.
env = { RUSTFLAGS = "-Z allow-features=c_variadic,allocator_api,coverage_attribute" }
command = "cargo"
args = ["llvm-cov", "@@split(COV_FLAGS, )", "--no-report"]
dependencies = ["individual-package-targets"]

[tasks.coverage-lcov]
description = "Generate an LCOV coverage report from collected data."
install_crate = false
clear = true
command = "cargo"
args = ["llvm-cov", "report", "--lcov", "--output-path", "${CARGO_MAKE_WORKSPACE_WORKING_DIRECTORY}/target/lcov.info"]

[tasks.coverage-html]
description = "Generate an HTML coverage report from collected data."
install_crate = false
clear = true
command = "cargo"
args = ["llvm-cov", "report", "--html", "--output-dir", "${CARGO_MAKE_WORKSPACE_WORKING_DIRECTORY}/target/coverage"]

[tasks.coverage]
description = "Build and run all tests and calculate coverage (runs test once and generates LCOV and HTML reports)."
dependencies = ["coverage-collect", "coverage-lcov", "coverage-html"]
clear = true

[tasks.coverage-filter]
description = "Generates the coverage filter to ignore coverage data for other packages."
private = true
script_runner = "@duckscript"
script = '''
package = get_env PACKAGE
members = get_env CARGO_MAKE_CRATE_WORKSPACE_MEMBERS

1 = split ${members} ,

2 = array
for member in ${1}
    if not contains ${member} ${package}
        array_push ${2} ${member}
    end


end

joined = array_join ${2} "|"

set_env PACKAGE_COVERAGE_FILTER ${joined}
release ${1}
release ${2}
release ${joined_args}
release ${joined}
'''

[tasks.coverage-fail-package]
private = true
install_crate = false
description = """Generates Code coverage for $(PACKAGE) and fails the build if coverage is below 80%."""
dependencies = ["coverage-filter"]
command = "cargo"
args = ["llvm-cov", "--package", "${PACKAGE}", "--fail-under-lines", "80", "--ignore-filename-regex", "${PACKAGE_COVERAGE_FILTER}"]

[tasks.coverage-fail]
description = """Runs coverage on one or all packages and fails if coverage is below 80%.

Examples:
    `cargo make coverage-fail`
    `cargo make coverage-fail patina_dxe_core`
"""
script_runner = "@duckscript"
script = '''
1 = get_env CARGO_MAKE_CRATE_WORKSPACE_MEMBERS
2 = split ${1} ,
3 = get_env CARGO_MAKE_TASK_ARGS

if not is_empty ${3}
    set_env PACKAGE ${3}
    cm_run_task coverage-fail-package
else
    for member in ${2}
        4 = split ${member} /
        5 = array_pop ${4}
        set_env PACKAGE ${5}
        cm_run_task coverage-fail-package
        release ${4}
        release ${5}
    end
endif

release ${1}
release ${2}
release ${3}
'''

[tasks.test-asan]
description = "Run tests with AddressSanitizer enabled. Example `cargo make test-asan`. Use `cargo make test-asan --print-dll-path` to print the ASan runtime DLL directory."
clear = true
script_runner = "@duckscript"
script = '''
os = os_family

if eq ${os} "windows"
    # ASan testing is only supported on Windows x64.
    # https://learn.microsoft.com/cpp/sanitizers/asan
    arch = get_env PROCESSOR_ARCHITECTURE
    if not eq ${arch} "AMD64"
        echo "test-asan is only supported on Windows x64. Detected arch: ${arch}. Skipping."
        exit 0
    end

    # The ASan runtime DLL (clang_rt.asan_dynamic-x86_64.dll) from the
    # MSVC toolchain must be on PATH for ASan-instrumented test binaries to run.
    #
    # Resolve the VC toolset path per vswhere docs:
    # https://github.com/microsoft/vswhere/wiki/Find-VC
    program_files_x86 = get_env "ProgramFiles(x86)"
    vswhere = set "${program_files_x86}/Microsoft Visual Studio/Installer/vswhere.exe"
    output = exec "${vswhere}" -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath
    install_dir = trim ${output.stdout}
    version = readfile "${install_dir}/VC/Auxiliary/Build/Microsoft.VCToolsVersion.default.txt"
    version = trim ${version}
    asan_dll_path = set "${install_dir}/VC/Tools/MSVC/${version}/bin/HostX64/x64"

    args = get_env CARGO_MAKE_TASK_ARGS
    if contains "${args}" "--print-dll-path"
        asan_dll_path = replace ${asan_dll_path} / \\
        echo "${asan_dll_path}"
        exit 0
    end

    current_path = get_env PATH
    set_env PATH "${asan_dll_path};${current_path}"
elseif eq ${os} "linux"
    # On Linux, the ASan runtime is resolved automatically by the linker.
    noop
else
    echo "test-asan is not supported on ${os}. Skipping."
    exit 0
end

set_env RUSTFLAGS "-Zsanitizer=address"
set_env RUSTDOCFLAGS "-Zsanitizer=address"
target = get_env CARGO_MAKE_RUST_TARGET_TRIPLE
exec --fail-on-error cargo test --target ${target} --workspace --features std
'''

[tasks.build-efi-exec]
command = "cargo"
args = ["build", "--bin", "${CARGO_BIN_NAME}", "--target", "${CARGO_BIN_TARGET}", "--no-default-features", "--features", "${COMBINED_FEATURES}", "@@split(NO_STD_FLAGS, )"]

[tasks.build-efi-prepare-features]
script_runner = "@duckscript"
script = '''
make_args = get_env CARGO_MAKE_TASK_ARGS
base_features = get_env CARGO_BIN_FEATURES

# Default to base features
combined_features = set ${base_features}

# Find arguments after --features and --exclude-features
features = set ""
exclude_features = set ""
if not is_empty ${make_args}
    args = split ${make_args} ";"
    found_feature_flag = set false
    found_exclude_flag = set false
    for arg in ${args}
        arg = trim ${arg}
        if eq ${arg} "--features"
            found_feature_flag = set true
        elseif eq ${arg} "--exclude-features"
            found_exclude_flag = set true
        elseif ${found_feature_flag}
            features = set ${arg}
            found_feature_flag = set false
        elseif ${found_exclude_flag}
            exclude_features = set ${arg}
            found_exclude_flag = set false
        end
    end
end

if is_empty ${base_features}
    combined_features = set ${features}
elseif is_empty ${features}
    combined_features = set ${base_features}
else
    combined_features = concat ${base_features} "," ${features}
end

# Remove excluded features from combined features using positional string
# replacement. Each replace targets a different position in the comma-separated
# list (middle, end, start, only item) with the most specific patterns first to
# avoid leaving stray commas.
#
# Note: This uses substring matching, so an exclude like "x64" would also match
# inside "x64_extended". For now, ensure feature names are not substrings of each
# other.
if not is_empty ${exclude_features}
    excludes = split ${exclude_features} ","
    for exclude in ${excludes}
        exclude = trim ${exclude}
        combined_features = replace ${combined_features} ",${exclude}," ","
        combined_features = replace ${combined_features} ",${exclude}" ""
        combined_features = replace ${combined_features} "${exclude}," ""
        combined_features = replace ${combined_features} "${exclude}" ""
    end
    release ${excludes}
end

# Set the combined features for the build-efi-exec task
set_env COMBINED_FEATURES ${combined_features}
'''

[tasks.build-efi]
dependencies = ["build-efi-prepare-features"]
run_task = "build-efi-exec"

[tasks.msvm-x64]
description = """Builds the DEBUG MSVM X64 UEFI firmware."""
env = { CARGO_BIN_NAME = "msvm_x64_dxe_core", "RUSTC_PROFILE" = "dev", CARGO_BIN_TARGET = "x86_64-unknown-uefi", CARGO_BIN_FEATURES = "${BASE_FEATURES},x64,build_debugger", CARGO_TARGET_X86_64_UNKNOWN_UEFI_RUSTFLAGS = "-C link-arg=/PDBALTPATH:msvm_x64_dxe_core.pdb" }
run_task = "build-efi"

[tasks.msvm-x64-release]
description = """Builds the RELEASE MSVM X64 UEFI firmware."""
env = { CARGO_BIN_NAME = "msvm_x64_dxe_core", "RUSTC_PROFILE" = "release", CARGO_BIN_TARGET = "x86_64-unknown-uefi", CARGO_BIN_FEATURES = "${BASE_FEATURES},x64", CARGO_TARGET_X86_64_UNKNOWN_UEFI_RUSTFLAGS = "-C link-arg=/PDBALTPATH:msvm_x64_dxe_core.pdb" }
run_task = "build-efi"

[tasks.msvm-aarch64]
description = """Builds the DEBUG MSVM AARCH64 UEFI firmware."""
env = { CARGO_BIN_NAME = "msvm_aarch64_dxe_core", "RUSTC_PROFILE" = "dev", CARGO_BIN_TARGET = "aarch64-unknown-uefi", CARGO_BIN_FEATURES = "${BASE_FEATURES},aarch64,build_debugger", CARGO_TARGET_AARCH64_UNKNOWN_UEFI_RUSTFLAGS = "-C link-arg=/PDBALTPATH:msvm_aarch64_dxe_core.pdb" }
run_task = "build-efi"

[tasks.msvm-aarch64-release]
description = """Builds the RELEASE MSVM AARCH64 UEFI firmware."""
env = { CARGO_BIN_NAME = "msvm_aarch64_dxe_core", "RUSTC_PROFILE" = "release", CARGO_BIN_TARGET = "aarch64-unknown-uefi", CARGO_BIN_FEATURES = "${BASE_FEATURES},aarch64", CARGO_TARGET_AARCH64_UNKNOWN_UEFI_RUSTFLAGS = "-C link-arg=/PDBALTPATH:msvm_aarch64_dxe_core.pdb" }
run_task = "build-efi"

[tasks.doc]
description = "Builds all rust documentation in the workspace. Example `cargo make doc`"
command = "cargo"
args = ["doc", "@@split(INDIVIDUAL_PACKAGE_TARGETS, )", "--no-deps"]

[tasks.doc-open]
description = "Builds all rust documentation in the workspace and opens the documentation Example `cargo make doc-open`"
command = "cargo"
args = ["doc", "@@split(INDIVIDUAL_PACKAGE_TARGETS, )", "--open"]

[tasks.clippy]
description = "Run cargo clippy for the host target and both UEFI cross-compilation targets."
clear = true
run_task = [{ name = ["clippy-x64", "clippy-aarch64", "clippy-std"], parallel = true }]

[tasks.clippy-x64]
description = "Run cargo clippy for x86_64-unknown-uefi."
private = true
command = "cargo"
args = ["clippy", "--target", "x86_64-unknown-uefi", "--features", "ci_features,x64", "@@split(NO_STD_FLAGS, )", "--", "-D", "warnings"]

[tasks.clippy-aarch64]
description = "Run cargo clippy for aarch64-unknown-uefi."
private = true
command = "cargo"
args = ["clippy", "--target", "aarch64-unknown-uefi", "--features", "ci_features,aarch64", "@@split(NO_STD_FLAGS, )", "--", "-D", "warnings"]

[tasks.clippy-std]
description = "Run cargo clippy for the host target (library and tests only)."
private = true
command = "cargo"
args = ["clippy", "--tests", "--", "-D", "warnings"]

[tasks.fmt]
description = "Run cargo format."
clear = true
command = "cargo"
args = ["fmt", "--all"]

[tasks.fmt-check]
description = "Check cargo format without modifying files."
clear = true
command = "cargo"
args = ["fmt", "--all", "--check"]

[tasks.doc-test]
description = "Run documentation tests."
clear = true
command = "cargo"
args = ["test", "--doc"]

[tasks.cspell]
description = "Run cspell for spell checking." # npm install -g cspell@latest
script = "cspell --quiet  --no-progress --no-summary  --dot --gitignore -e \"{.git/**,.github/**,.vscode/**}\" ."

[tasks.deny]
description = "Run cargo deny."
install_crate = false
clear = true
command = "cargo"
args = ["deny", "check"]

[tasks.bloat-msvm-x64-exec]
private = true
command = "cargo"
args = ["bloat", "--target", "x86_64-unknown-uefi", "--features", "x64", "--bin", "msvm_x64_dxe_core", "--profile", "${RUSTC_PROFILE}", "@@split(CLEANED_ARGS, )"]

[tasks.bloat-msvm-x64]
description = "Run cargo bloat for msvm-x64 (x86_64-unknown-uefi). Examples: `cargo make bloat-msvm-x64 -- --crates`, `cargo make bloat-msvm-x64 -- --crates -n 40`."
script_runner = "@duckscript"
script = '''
args = get_env CARGO_MAKE_TASK_ARGS

# Clean up the args by replacing semicolons with spaces
if not is_empty ${args}
    clean_args = replace ${args} ";;--" " --"
    clean_args = replace ${clean_args} ";;" " "
    clean_args = replace ${clean_args} ";" " "
    # Remove leading/trailing whitespace and -- prefix
    clean_args = trim ${clean_args}
    clean_args = replace ${clean_args} "-- " ""
    set_env CLEANED_ARGS ${clean_args}
else
    set_env CLEANED_ARGS ""
end

cm_run_task bloat-msvm-x64-exec
'''

[tasks.bloat-msvm-aarch64-exec]
private = true
command = "cargo"
args = ["bloat", "--target", "aarch64-unknown-uefi", "--features", "aarch64", "--bin", "msvm_aarch64_dxe_core", "--profile", "${RUSTC_PROFILE}", "@@split(CLEANED_ARGS, )"]

[tasks.bloat-msvm-aarch64]
description = "Run cargo bloat for msvm-aarch64 (aarch64-unknown-uefi). Examples: `cargo make bloat-msvm-aarch64 -- --crates`, `cargo make bloat-msvm-aarch64 -- --crates -n 40`."
script_runner = "@duckscript"
script = '''
args = get_env CARGO_MAKE_TASK_ARGS

# Clean up the args by replacing semicolons with spaces
if not is_empty ${args}
    clean_args = replace ${args} ";;--" " --"
    clean_args = replace ${clean_args} ";;" " "
    clean_args = replace ${clean_args} ";" " "
    # Remove leading/trailing whitespace and -- prefix
    clean_args = trim ${clean_args}
    clean_args = replace ${clean_args} "-- " ""
    set_env CLEANED_ARGS ${clean_args}
else
    set_env CLEANED_ARGS ""
end

cm_run_task bloat-msvm-aarch64-exec
'''

[tasks.all]
description = "Run all tasks for PR readiness."
dependencies = [
    "fmt-check",
    "deny",
    "clippy",
    "cspell",
    "check-no-default-features",
    "msvm-aarch64",
    "msvm-aarch64-release",
    "msvm-x64",
    "msvm-x64-release",
    "test",
    "coverage",
    "doc-test",
    "doc",
]
